<x-layouts.app :narrow="true" :meta="$meta">@include('community.nav')<h1 class="text-hero">{{ $meta->heading }}</h1>
    <ul class="my-6 divide-y divide-line">@foreach($followers as $follower)<li class="flex min-h-16 items-center justify-between gap-4 py-4"><span>@if($follower['handle'])<a class="underline" href="{{ route('community.profile', $follower['handle']) }}">{{ $follower['display_name'] }}</a>@else{{ $follower['display_name'] }}@endif</span><form method="post" action="{{ route('community.block', $follower['user_id']) }}">@csrf<button class="min-h-11 text-sm underline">{{ __('community.block') }}</button></form></li>@endforeach</ul>
    <x-pagination :paginator="$pagination" />
    <h2 class="mt-10 text-section">{{ __('community.blocks') }}</h2><p class="mt-3 text-sm text-ink-muted">{{ __('community.block_help') }}</p><ul class="mt-4 divide-y divide-line">@foreach($blocks as $block)<li class="flex min-h-16 items-center justify-between gap-4 py-4"><span>{{ $block['display_name'] }}</span><form method="post" action="{{ route('community.unblock', $block['user_id']) }}">@csrf @method('DELETE')<button class="min-h-11 underline">{{ __('community.unblock') }}</button></form></li>@endforeach</ul>
</x-layouts.app>
