<x-layouts.app :narrow="true" :meta="$meta"><div class="mx-auto w-full max-w-4xl">@include('carpool.nav')
<h1 class="text-hero">{{ __('carpool.messages') }}</h1>
<a class="my-5 inline-flex min-h-12 items-center underline" href="{{ request()->fullUrlWithQuery(['archived' => $archived ? 0 : 1, 'page' => 1]) }}">{{ __($archived ? 'carpool.active_chats' : 'carpool.archived_chats') }}</a>
<div class="mt-8 divide-y divide-line">@forelse($chats as $chat)<article class="space-y-3 py-6"><p class="text-sm text-ink-muted">{{ $chat['offer']['departure_label'] }} · {{ $chat['offer']['leg_label'] }}</p><h2 class="text-section">{{ $chat['offer']['event_title'] }}</h2><p>{{ $chat['ride']['is_driver'] ? $chat['ride']['requester']['name'] : $chat['offer']['driver']['name'] }} · {{ $chat['ride']['status_label'] }} @if($chat['unread'])<span class="font-bold text-accent">({{ $chat['unread'] }})</span>@endif</p>@if($chat['readable'])<x-button variant="secondary" :href="route('carpool.chat',$chat['id'])">{{ __('carpool.open_chat') }}</x-button>@else<p class="text-sm text-ink-muted">{{ __('carpool.chat_hidden') }}</p>@endif</article>@empty<p class="py-8 text-ink-muted">{{ __('carpool.empty_messages') }}</p>@endforelse</div>
@include('carpool.partials.pages')
</div></x-layouts.app>
