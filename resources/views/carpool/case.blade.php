<x-layouts.app :narrow="true" :meta="$meta"><div class="mx-auto w-full max-w-4xl">@include('carpool.nav')
<h1 class="text-hero">{{ __('carpool.case',['id'=>$case_id]) }}</h1><p class="my-5 text-sm text-ink-muted">{{ $case_status }}</p>@if($body)<p class="my-6 whitespace-pre-line">{{ $body }}</p>@endif
@if(count($messages) === 50)<a class="inline-flex min-h-12 items-center underline" href="{{ request()->fullUrlWithQuery(['before' => $messages[0]['id']]) }}">{{ __('carpool.older') }}</a>@endif
<div class="my-6 space-y-4">@foreach($messages as $message)<article class="border border-line p-5 {{ $message['mine'] ? 'ml-6' : '' }}"><p class="mb-2 text-sm font-semibold">{{ $message['mine'] ? __('account.nav.profile') : __('carpool.support') }}</p><p class="whitespace-pre-line break-words">{{ $message['body'] }}</p></article>@endforeach</div><form method="post" action="{{ route('carpool.case.reply',$case_id) }}" class="max-w-2xl space-y-4">@include('carpool.partials.key')<label class="block">{{ __('carpool.reply') }}<textarea required name="body" minlength="5" maxlength="2000" rows="4" class="mt-3 w-full border border-line bg-canvas p-4"></textarea></label><x-button type="submit">{{ __('carpool.send') }}</x-button></form>
</div></x-layouts.app>
