name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

# Sola lettura per difetto. Il job di rilascio se lo alza da se', perche' e'
# l'unico che scrive — e scrive una cosa sola: gli asset compilati sul ramo
# `assets`, che il server poi scarica.
permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

# Le action di terze parti sono pinnate al commit SHA, non al tag: un tag e
# mutabile e chi lo controlla puo spostarlo su codice diverso da quello verificato.
jobs:
  quality:
    name: Lint e analisi statica
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - uses: shivammathur/setup-php@b604ade2a87db23f8871b7182e69ec5e75effb45 # v2
        with:
          php-version: '8.4'
          extensions: mbstring, intl, gd, imagick, zip, bcmath, pdo_mysql, exif
          coverage: none

      - uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: ~/.composer/cache
          key: composer-${{ hashFiles('composer.lock') }}

      - run: composer install --no-interaction --prefer-dist --no-progress

      - name: Pint
        run: ./vendor/bin/pint --test

      - name: Vulnerabilità delle dipendenze PHP
        run: composer audit --no-interaction

      - name: PHPStan
        run: ./vendor/bin/phpstan analyse --memory-limit=1G --no-progress

  tests:
    name: Test (${{ matrix.shard }}/3)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        shard: [1, 2, 3]
    services:
      mariadb:
        # stessa major della produzione: MySQL e MariaDB divergono sulle funzioni
        # spaziali e sull'ordine degli assi con SRID 4326
        image: mariadb:10.11
        env:
          MARIADB_ROOT_PASSWORD: root
          MARIADB_DATABASE: eventi_test
        ports: ['3306:3306']
        options: >-
          --health-cmd="healthcheck.sh --connect --innodb_initialized"
          --health-interval=10s --health-timeout=5s --health-retries=10

    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - uses: shivammathur/setup-php@b604ade2a87db23f8871b7182e69ec5e75effb45 # v2
        with:
          php-version: '8.4'
          extensions: mbstring, intl, gd, imagick, zip, bcmath, pdo_mysql, exif
          coverage: none

      - uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: ~/.composer/cache
          key: composer-${{ hashFiles('composer.lock') }}

      - run: composer install --no-interaction --prefer-dist --no-progress

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '22'
          cache: npm

      - run: npm ci
      - run: npm run build

      # `mariadb-client` e non il `mysqldump` gia' presente sull'immagine:
      # quello e' il client di MySQL 8, che contro un server MariaDB fallisce
      # su `--column-statistics` — un'opzione che MySQL manda di suo e MariaDB
      # non conosce. Il dump esce con stato 1 e il backup non viene creato.
      # Spatie cerca `mariadb-dump` per primo su una connessione `mariadb`.
      - name: Client MariaDB per il dump del database
        run: sudo apt-get update -qq && sudo apt-get install -y -qq mariadb-client

      - name: Prepara ambiente
        run: |
          cp .env.example .env
          php artisan key:generate

      - name: Pest
        run: ./vendor/bin/pest --ci --shard=${{ matrix.shard }}/3 --log-junit storage/logs/junit.xml
        env:
          DB_CONNECTION: mariadb
          DB_HOST: 127.0.0.1
          DB_PORT: 3306
          DB_DATABASE: eventi_test
          DB_USERNAME: root
          DB_PASSWORD: root

      - name: Vulnerabilità JavaScript, inclusi gli strumenti di build
        run: npm audit --audit-level=high

      - name: Referto dei test
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: pest-report-${{ matrix.shard }}
          path: storage/logs/junit.xml
          retention-days: 14

      - name: Conserva gli asset che hanno superato i test
        if: matrix.shard == 1
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: tested-build
          path: public/build
          retention-days: 14
          if-no-files-found: error

  lighthouse:
    name: Lighthouse
    runs-on: ubuntu-latest
    timeout-minutes: 25
    services:
      mariadb:
        image: mariadb:10.11
        env:
          MARIADB_ROOT_PASSWORD: root
          MARIADB_DATABASE: eventi_lighthouse
        ports: ['3306:3306']
        options: >-
          --health-cmd="healthcheck.sh --connect --innodb_initialized"
          --health-interval=10s --health-timeout=5s --health-retries=10

    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - uses: shivammathur/setup-php@b604ade2a87db23f8871b7182e69ec5e75effb45 # v2
        with:
          php-version: '8.4'
          extensions: mbstring, intl, gd, imagick, zip, bcmath, pdo_mysql, exif
          coverage: none

      - uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: ~/.composer/cache
          key: composer-${{ hashFiles('composer.lock') }}

      - run: composer install --no-interaction --prefer-dist --no-progress

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '22'
          cache: npm

      - run: npm ci
      - run: npm run build

      - name: Prepara ambiente
        env:
          DB_DATABASE: eventi_lighthouse
          DB_PASSWORD: root
        run: |
          cp .env.example .env
          sed -i "s|^DB_PORT=.*|DB_PORT=3306|" .env
          sed -i "s|^DB_DATABASE=.*|DB_DATABASE=${DB_DATABASE}|" .env
          sed -i "s|^DB_PASSWORD=.*|DB_PASSWORD=${DB_PASSWORD}|" .env
          sed -i "s|^APP_DEBUG=.*|APP_DEBUG=false|" .env
          sed -i "s|^APP_URL=.*|APP_URL=http://127.0.0.1:8080|" .env
          php artisan key:generate
          php artisan storage:link

      # Il sito misurato deve avere dentro qualcosa: una pagina vuota prende
      # novanta su novanta e non dice niente. Il seeder e' lo stesso della
      # demo, con centocinquanta date sui prossimi trenta giorni.
      # **Il delegato che fa scrivere gli AVIF a ImageMagick.**
      #
      # L'immagine del runner porta ImageMagick ma non `libheif`, e senza
      # quello `AvifSupport::available()` risponde di no: si generano solo i
      # WebP, e la stessa locandina che in produzione pesa 54 KB ne pesa 274.
      # Il referto lo riporta come «LCP alto», che manda a cercare nel codice
      # un problema che sta nell'ambiente.
      #
      # In produzione tutte le 218 immagini hanno la variante AVIF: senza
      # questo passo il banco misura un sito che non esiste.
      - name: Installa il supporto AVIF per ImageMagick
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends libheif1 libheif-plugin-aomenc 2>/dev/null \
            || sudo apt-get install -y --no-install-recommends libheif1

      - name: Popola il database
        run: php artisan migrate:fresh --seed --force

      # Le varianti WebP e AVIF nascono da lavori in coda (§12.1). Senza questo
      # passaggio il sito servirebbe le locandine originali e la misura
      # boccerebbe una pipeline che in produzione funziona: e' la differenza
      # fra novanta e settantacinque, verificata.
      - name: Genera le varianti delle immagini
        run: php artisan queue:work --stop-when-empty --tries=1 --quiet

      # **Le locandine devono uscire in AVIF, come in produzione.**
      #
      # Se ImageMagick qui non sa scriverlo, `AvifSupport::available()` risponde
      # di no e si generano solo i WebP: la stessa locandina che in produzione
      # pesa 54 KB ne pesa 274, e su banda simulata sono un secondo e mezzo di
      # rete occupata. Il referto pero' non dice «manca l'AVIF»: dice «LCP
      # alto», e manda a cercare nel posto sbagliato — e' costato un'indagine.
      #
      # In produzione sono 218 immagini su 218 ad avere la variante AVIF: un
      # banco che ne ha zero non sta misurando questo sito.
      #
      # Non fa fallire il lavoro: se un giorno il runner perdesse il delegato,
      # meglio una misura pessimista con l'avviso scritto che una pipeline
      # bloccata su qualcosa che non riguarda il codice.
      - name: Controlla che l'AVIF sia davvero disponibile
        run: |
          avif=$(find storage/app/public -name '*-avif.avif' 2>/dev/null | wc -l | tr -d ' ')
          webp=$(find storage/app/public -name '*.webp' 2>/dev/null | wc -l | tr -d ' ')

          echo "Varianti generate: ${webp} WebP, ${avif} AVIF."

          if [ "${avif}" -eq 0 ]; then
            echo "::warning title=AVIF non generato::ImageMagick su questo runner non scrive AVIF: le locandine escono solo in WebP e pesano da tre a cinque volte tanto. Le misure di Lighthouse che seguono sono piu' lente di quelle del sito vero, dove tutte le immagini hanno la variante AVIF."
            php -r 'echo "delegati di ImageMagick: ", implode(", ", array_slice((new Imagick)->queryFormats("*AVIF*") ?: ["nessuno"], 0, 5)), PHP_EOL;' || true
          fi

      # Il server PHP di sviluppo non e' il prodotto: niente compressione,
      # niente intestazioni di cache, ogni immagine attraverso PHP. Davanti gli
      # si mette nginx, che e' cio' che fa il server vero. L'immagine del
      # runner non lo porta, e con lui arriva `/etc/nginx/mime.types`, a cui il
      # modello fa riferimento.
      - name: Installa nginx
        run: sudo apt-get update -qq && sudo apt-get install -y --no-install-recommends nginx

      # **La stessa applicazione che riceve chi visita il sito.**
      #
      # In produzione il rilascio mette in cache configurazione, rotte e viste
      # (`deploy:pull` lo fa a ogni giro). Senza, ogni richiesta rilegge tutti
      # i file di config, ricostruisce la tabella delle rotte e ricompila i
      # Blade: qui valeva 451ms di attesa prima del primo byte, cioe' il 22%
      # del tempo di disegno misurato.
      #
      # Misurare senza equivale a bocciare il sito per la lentezza del banco di
      # prova. Non e' addolcire la misura: e' misurare la cosa giusta.
      - name: Prepara l'applicazione come in produzione
        run: php artisan optimize

      # Si avvia `php -S` a mano invece di `php artisan serve`, e non per
      # gusto: `artisan serve` e' un padre che legge lo standard output del
      # figlio attraverso una pipe, e quando la shell di QUESTO passo termina
      # la pipe si rompe. Da quel momento ogni risposta del sito esce con un
      # `Notice: Broken pipe` incollato davanti al `<!doctype>` — e Lighthouse
      # legge un `robots.txt` invalido e una pagina senza meta description.
      # Costava diciassette punti di SEO, e sembrava un difetto del sito.
      # Il router `server.php` legge `getcwd()`, quindi va lanciato da `public/`.
      - name: Avvia applicazione e server
        run: |
          mkdir -p "${RUNNER_TEMP}/nginx"
          sed -e "s|__ROOT__|${GITHUB_WORKSPACE}/public|g" \
              -e "s|__PORT__|8080|g" \
              -e "s|__APP_PORT__|8000|g" \
              -e "s|__MIME__|/etc/nginx/mime.types|g" \
              -e "s|__TMP__|${RUNNER_TEMP}/nginx|g" \
              .github/lighthouse/nginx.conf.template > "${RUNNER_TEMP}/nginx/nginx.conf"

          ROUTER=vendor/laravel/framework/src/Illuminate/Foundation/resources/server.php
          ( cd public && PHP_CLI_SERVER_WORKERS=8 \
            nohup php -d variables_order=EGPCS -S 127.0.0.1:8000 "${GITHUB_WORKSPACE}/${ROUTER}" \
            > "${RUNNER_TEMP}/php-server.log" 2>&1 < /dev/null & )

          nohup nginx -c "${RUNNER_TEMP}/nginx/nginx.conf" -p "${RUNNER_TEMP}/nginx" \
            > "${RUNNER_TEMP}/nginx.log" 2>&1 < /dev/null &

          # `if` e non `[ ... ] && break`: sotto `set -e` una lista `&&` che
          # torna falsa come ultimo comando di un giro interrompe il passo, e
          # il fallimento sembrerebbe del server invece che dell'attesa.
          code=000
          for _ in $(seq 1 30); do
            code=$(curl -sS -o /dev/null -w '%{http_code}' http://127.0.0.1:8080/ || echo 000)
            if [ "$code" = "200" ]; then break; fi
            sleep 1
          done
          [ "$code" = "200" ] || { echo "Il sito non risponde: $code"; cat "${RUNNER_TEMP}/nginx.log"; exit 1; }

          # Una pagina servita bene non comincia con un avviso di PHP: se
          # succede, il resto della misura non vale niente.
          curl -sS http://127.0.0.1:8080/ | head -c 15 | grep -q '<!DOCTYPE html>' \
            || { echo "La pagina non comincia con il doctype: qualcosa scrive prima dell'HTML"; exit 1; }

      # Lo slug di un evento nasce dal seeder e cambia a ogni esecuzione:
      # scriverlo nella configurazione significherebbe misurare un 404. Il feed
      # RSS elenca le date future, e il suo primo collegamento e' una scheda
      # evento viva.
      - name: Scegli la scheda evento da misurare
        run: |
          url=$(curl -sS http://127.0.0.1:8080/feed.rss \
            | grep -oE '<link>[^<]+/eventi/[^<]+</link>' | head -1 | sed -E 's|</?link>||g')
          [ -n "$url" ] || { echo "Nessun evento nel feed: il seeder non ha popolato nulla"; exit 1; }
          echo "Scheda misurata: $url"
          echo "LHCI_EVENT_URL=$url" >> "$GITHUB_ENV"

      - name: Lighthouse CI
        env:
          LHCI_BASE_URL: http://127.0.0.1:8080
          LHCI_OUTPUT_DIR: ${{ runner.temp }}/lighthouse
        run: npx lhci autorun --config=lighthouserc.cjs

      # I referti restano allegati anche quando le soglie passano: e' la sola
      # traccia di com'era il sito il giorno in cui e' stato misurato.
      - name: Allega i referti
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: lighthouse
          path: ${{ runner.temp }}/lighthouse
          retention-days: 14
          if-no-files-found: warn

  android:
    name: Android unit test e compilazione
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - uses: actions/setup-java@cf277c60eb25467037889841efdb72551f06f6c3
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle
      - run: ./gradlew testDebugUnitTest compileDebugKotlin
        working-directory: android
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        if: always()
        with:
          name: android-test-reports
          path: android/app/build/reports/tests
          retention-days: 14

  deploy:
    name: Deploy su eventi.fabiodalez.it
    needs: [quality, tests, lighthouse, android]
    if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
    # Some x86 runner network ranges receive an unrelated hosting certificate.
    # ARM connectivity was verified with strict TLS. Never bypass certificate checks.
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 25
    environment:
      name: production
      url: https://eventi.fabiodalez.it
    concurrency:
      group: deploy-production
      cancel-in-progress: false
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
        with:
          name: tested-build
          path: public/build
      - name: Pubblica soltanto gli asset dello stesso commit verificato
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          current=$(gh api "repos/$GITHUB_REPOSITORY/git/ref/heads/main" --jq .object.sha)
          test "$current" = "$GITHUB_SHA" || { echo "main è avanzato: attendo la nuova CI."; exit 1; }
          node -e 'require("fs").writeFileSync("public/build/release.json", JSON.stringify({sha:process.env.GITHUB_SHA,run_id:process.env.GITHUB_RUN_ID,checks:"passed"}))'
          cd public/build
          git init -q
          git switch -c assets
          git add -A
          git -c user.name=deploy -c user.email=deploy@eventi.fabiodalez.it commit -m "asset compilati per $GITHUB_SHA"
          git push -f "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" HEAD:assets
      - name: Chiedi al server di aggiornarsi
        env:
          DEPLOY_URL: ${{ secrets.DEPLOY_HOOK_URL }}
          DEPLOY_SECRET: ${{ secrets.DEPLOY_HOOK_SECRET }}
        run: |
          # Il cron è il fallback, ma l'esito finale sotto è obbligatorio.
          curl --ipv4 --fail --silent --show-error --connect-timeout 10 --max-time 120 -X POST "$DEPLOY_URL" \
            -H "X-Secret-Token: $DEPLOY_SECRET" -H "Accept: application/json" \
            || echo "::warning::Innesco non raggiungibile; attendo il cron e verifico il rilascio."
      - name: Verifica il commit effettivamente online e le pagine pubbliche
        env:
          EXPECTED_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          ready=false
          deadline=$((SECONDS + 600))
          while [ "$SECONDS" -lt "$deadline" ]; do
            actual=$(curl --ipv4 --fail --silent --show-error --connect-timeout 5 --max-time 10 "https://eventi.fabiodalez.it/release-status?run=$GITHUB_RUN_ID" | jq -r .sha || true)
            if [ "$actual" = "$EXPECTED_SHA" ]; then ready=true; break; fi
            sleep 10
          done
          test "$ready" = true || { echo "::error::Il server non conferma il commit verificato."; exit 1; }
          for path in / /eventi /locali /sitemap.xml /api/v1/home /admin/login; do
            curl --ipv4 --fail --silent --show-error --retry 3 --connect-timeout 5 --max-time 30 "https://eventi.fabiodalez.it$path" -o /dev/null
          done
          echo "### Rilascio verificato: $EXPECTED_SHA" >> "$GITHUB_STEP_SUMMARY"
